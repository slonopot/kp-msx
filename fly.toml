app = "kp-msx"

[processes]
app = "uvicorn api:app --host 0.0.0.0 --port 10000"

[http_service]
auto_start_machines = true
auto_stop_machines = true
force_https = true
internal_port = 10000
min_machines_running = 0
processes = [ "app" ]

[[vm]]
cpu_kind = "shared"
cpus = 1
memory_mb = 512